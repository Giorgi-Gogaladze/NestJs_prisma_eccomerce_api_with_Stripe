model Cart {
  id         String  @id    @default(uuid()) @db.Uuid()
  checkedOut Boolean   @default(flase)
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt

  @@map("carts")
}